# Ozon

Симуляция и визуализация процесса непрерывной озонной очистки воды в формате высокодетализированной операторской панели.

## Запуск

Откройте `index.html` в браузере или запустите локальный сервер:

```bash
python -m http.server 8000
```

После этого перейдите на `http://localhost:8000`.
